# Frontend (Next.js) — UI only. It proxies /api/* to the Python backend.
# BACKEND_URL is resolved at build time (docker-compose sets it to http://backend:8000).
# For local `npm run dev` it defaults to http://localhost:8000; override here if needed.
BACKEND_URL=http://localhost:8000

# Backend configuration lives in backend/.env.example.
